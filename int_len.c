#include "main.h"

/**
 * _intlen_recursion - returns the length of a int input.
 * @num: int
 * Return: the length of a int.
 */
int _intlen_recursion(int num)
{
	if (num == 0)
		return (0);
	else
		return (1 + _strlen_recursion(s + 1));
}

